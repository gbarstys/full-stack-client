import { Container, styled } from '@mui/material';

const LeashContainer = styled(Container)({
  display: 'flex',
  padding: '20px 0',
  alignItems: 'center',
  flexWrap: 'wrap',
  gap: '20px',
});

export default LeashContainer;
